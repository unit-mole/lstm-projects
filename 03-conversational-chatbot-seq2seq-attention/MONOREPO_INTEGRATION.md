# Monorepo Integration

Copy these items into the existing root `lstm-projects` repository:

```text
.github/workflows/03-conversational-chatbot-seq2seq-attention.yml
03-conversational-chatbot-seq2seq-attention/
README.md
```

The updated root README adds Project 03 while retaining Projects 01 and 02.

After copying, run:

```bat
git add .
git status
git commit -m "Add Seq2Seq attention chatbot project"
git push
```

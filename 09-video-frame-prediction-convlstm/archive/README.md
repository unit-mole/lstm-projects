# Archive

This folder preserves the original notebook, original metadata, and the detailed project specification supplied before the portfolio refactor. The production-facing project uses the modular code in `src/`, the cleaned notebook in `notebooks/`, and the saved model in `models/`.

Archived material is retained for traceability and should not be used as the Streamlit entrypoint.

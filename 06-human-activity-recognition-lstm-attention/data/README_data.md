# Data Guide

The full project uses a deterministic synthetic Human Activity Recognition dataset. No personal, wearable-device, or subject-identifying data is included. Each inference CSV must contain 80 ordered rows and these six columns: `acc_x`, `acc_y`, `acc_z`, `gyro_x`, `gyro_y`, and `gyro_z`.
